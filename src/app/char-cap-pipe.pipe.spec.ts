import { CharCapPipePipe } from './char-cap-pipe.pipe';

describe('CharCapPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CharCapPipePipe();
    expect(pipe).toBeTruthy();
  });
});
